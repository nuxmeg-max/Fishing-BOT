// public/config.js — di-load sebelum game script
// Vercel akan inject env vars via build step atau ini diisi manual

window.BOT_WEBHOOK_URL  = "https://YOUR-BOT-DOMAIN.com";  // ← ganti dengan domain/IP bot kamu
window.WEBHOOK_SECRET   = "capitano-mancing-2026";        // ← samakan dengan FISHING_SECRET di bot
